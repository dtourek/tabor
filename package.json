{
  "name": "tabor",
  "version": "1.0.2",
  "description": "Bunch of util functions",
  "main": "lib/index.js",
  "scripts": {
    "prepack": "yarn build && yarn test",
    "build": "rm -rf lib && tsc",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.5.1",
    "jest": "^28.1.0",
    "ts-jest": "^28.0.3",
    "typescript": "^4.7.2"
  }
}
